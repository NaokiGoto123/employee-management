<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Attendances</h1>
    <%= link_to 'New attendance', new_attendance_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="attendances" class="min-w-full">
    <%= render partial: 'attendances/attendance', collection: @attendances %>
  </div>
</div>
